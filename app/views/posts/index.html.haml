#tag_cloud
  - tag_cloud Post.tag_counts, %w{s m l} do |tag, css_class|
    = link_to tag.name, tag_path(tag.name), class: css_class
%br


- [@posts].each do |posts|
  - posts.each do |post|
    .post
      .posttitlelink
        = link_to post.title, post
      %br
      %br
      = link_to image_tag(post.photo), post

      %br

 